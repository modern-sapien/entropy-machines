// Placeholder — ready issues, claim status, and per-doc answered counts wire
// up to /api/dashboard in i-react-dashboard once i-server-api exists.
export function DashboardPage() {
  return (
    <section className="page">
      <h1>Dashboard</h1>
      <p className="sub">Ready issues, claim status, and answered counts land here in i-react-dashboard.</p>
    </section>
  );
}
