// Placeholder — list/board toggle and filters over /api/issues land in
// i-react-tracker once i-server-api exists.
export function TrackerPage() {
  return (
    <section className="page">
      <h1>Tracker</h1>
      <p className="sub">Issue list/board with filters lands here in i-react-tracker.</p>
    </section>
  );
}
